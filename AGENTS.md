# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## Prototype-specific decisions

- Build the landing page one section at a time and stop for review after each completed section.
- The source of truth for the current hero is Figma file `MNdUi7WoBwyzjLjYmtwBla`, node `265:2228`.
- Match the desktop Figma frame at `1440 × 1082` before judging responsive variants.
- The current hero defaults to Arabic and RTL from Figma node `265:2228`, with a working English/LTR language state aligned to node `175:429`.
- Preserve the exact Figma palette, type scale, spacing, radii, and visible copy. Do not add unapproved content.
- The hero background should move very subtly, and the navbar, badge, copy, CTA, and product preview should reveal with restrained, professional Framer-like entrance motion. Respect prefers-reduced-motion.
- The language control must switch the hero between complete Arabic/RTL and English/LTR states; it must not be decorative.
- Use the exact Figma-exported logo mark, gradient artwork, and dashboard screenshot stored in `public/assets`.
- The implementation must remain usable and visually composed from 320px mobile widths through large desktop screens.
- At the 1440px Figma target, the desktop navbar is 1170px wide and centered at x=135, while the product-preview frame is 1260px wide and begins at x=90.
- The approved company-logo section source is Figma node `242:4560`: preserve its 1440 x 134 white band, 48px vertical padding, exact exported Dell, Zendesk, Rakuten, Pacific Funds, NCR, Lattice, and TED artwork and dimensions, and 40% logo opacity. Animate the exact sequence as a seamless CSS-only infinite marquee with reduced-motion support.
- The booking-control section source is Figma node `249:1685`: match the 1440 x 712 white section, 90px vertical padding, 990px inner content, exact Arabic copy, four exported icons, and the exported 260 x 300 center illustration. Keep the section bilingual and responsive. On viewport entry, reveal the heading, side columns, and center illustration once with restrained Framer-like staggered motion; respect reduced motion.
- Responsive text must remain within the viewport at every intermediate width, not only named device breakpoints. The booking-control section switches away from its fixed three-column desktop layout at 1000px; from 1101px through 1439px it keeps three columns with fluid outer padding and no inner padding, while 1440px retains the exact Figma geometry. The header switches to its compact menu at 1100px, reveal transforms must not create horizontal overflow, and hero headings must keep a fluid full-width flex basis.
- The approved post-booking scroll showcase follows Figma node `249:1850` and the user scroll-story reference. Use only the exact exported dashboard, gradient, cursor, device controls, and feature icons; Arabic typography must use the local `Thmanyah` Regular/Medium font. The story remains three-phase (partial mobile, enlarged mobile, full web), with device controls and timed feature tracks active only in phase three. Until the final video arrives, animate the exact Figma dashboard asset subtly rather than substituting generated imagery.
- In the scroll showcase, the heading, description, and feature row must stay completely hidden in phases one and two. They enter or exit with one short phase-triggered animation at the phase-three boundary; opacity must never be scrubbed continuously against scroll progress. Use one identical live web/mobile selector in every phase: 84x44 outer size, 36x36 buttons, the same opaque background, position, hover, and selected states. It must cover the selector baked into the mobile reference so no duplicate or shape change is visible.
- All landing-page animation durations and stagger delays are intentionally 15% slower than their original approved values. The scroll-driven showcase also uses 15% more scroll distance so its scale progression feels consistently slower; keep reduced-motion behavior effectively instant.
- The scroll-driven showcase is intentionally 10% heavier than the already-slowed motion system: use 455.4svh on desktop and 417.45svh on compact layouts. This applies only to the showcase progression distance, not native browser wheel physics or unrelated animations.

- The approved “Why ViaBook?” section source is Figma node `271:7336`: at 1440px use a 1170px content width, 90px/135px section padding, 48px header-to-grid gap, four equal 281x192 cards with 16px gaps, 12px radii, and the exact four exported Figma icons. Keep Arabic/RTL and English/LTR content, switch to two columns below 1100px and one column below 620px, and reveal the header/cards once on viewport entry using the established 15%-slower motion timings.

- The approved “All tools” section source is Figma node `242:4808`: match the 1440x1416 frame, 1170px content, 184px header, 48px content gap, two 494px card rows with 16px gaps, top widths 527/627 and bottom widths 687/467. Use the exact Figma-rendered card artwork as the visual source while keeping live bilingual captions above opaque fades. Preserve these proportions fluidly from 1101–1439px, use two responsive columns at 1100px and below, stack at 720px, and reveal the header/cards once with the established 15%-slower motion.

- The approved "Scale your business" section source is Figma node `271:6621`: at 1440px preserve the 722px section, 1170x542 inner frame, 594x542 map card, 32px gap, 544px copy column, exact exported 462x411 Saudi dot map and three exported icons. Keep the Figma title on exactly two non-wrapping desktop lines, switch to a stacked layout at 1100px, allow mobile wrapping below 621px, support Arabic/RTL and English/LTR, and use the established one-time 15%-slower reveal motion.
- The approved FAQ section source is Figma node `242:4693`: preserve the 1440x692 frame, 900px content width, 72px header, 40px gap, exact 400px accordion initial state, and exported chevron asset. Keep the first item open initially, allow one item open at a time, provide useful Arabic/English answers, and preserve accessible `aria-expanded`/`aria-controls` states with the established 15%-slower reveal and accordion motion.
- The approved final CTA section source is Figma node 242:4715: preserve the 1440x506 frame, 90px/135px outer padding, 1170x326 navy card, 12px radius, exact Figma dashboard and gradient assets, and the original Arabic copy/button geometry. Keep the CTA functional, support Arabic/RTL and English/LTR, stack the composition below 1101px without overflow, and use the established one-time 15%-slower reveal plus a very subtle reduced-motion-safe gradient drift.
- The approved footer source is Figma node 242:4731. Keep its compact SaaS structure rather than expanding into a mega-footer: preserve the 1440x481 frame, 1170x301 inner layout, 361x127 two-column link group, divider at x=315, exact logo/social/mail/arrow assets, and bottom utility row. Replace Figma placeholder copy with real Explore and Support navigation plus copyright, privacy, and terms; support Arabic/RTL and English/LTR, real in-page/mail/social links, and the established 15%-slower reveal motion.

- The responsive header uses a Framer-inspired full-screen menu at 1100px and below: no circular button surface, one stable two-stroke icon morphing directly to/from X, a ViaBook-navy viewport overlay, staggered navigation/actions, body scroll lock, Escape/link/language close behavior, and reduced-motion support. Do not reintroduce a third stroke, separate List/X glyph swapping, or a rounded dropdown card.
- The navbar stays visible and visually integrated with the hero at the top. After scrolling it becomes a full-viewport-width transparent glass strip, never a floating box: no outer radius, no side margins on the glass layer, a restrained product-style gradient, no bottom stroke, no shadow, a clearly visible 42px backdrop blur layered above page content, and slightly increased vertical padding. Its foreground adapts to the underlying section (white over dark, navy over light), while the centered content keeps the approved 1170px desktop geometry. Below the hero, 18px of downward scroll hides both navbar and glass; reversing upward by 8px reveals them, the first 80px of the page always remain visible, and opening the compact menu forces the navbar visible. The compact full-screen mobile menu remains navy.


- Responsive media must preserve intrinsic proportions: use contain/cover instead of fill whenever a card or icon changes aspect ratio. The scroll showcase uses a shorter story at <=1000px (300svh) and <=700px (240svh); on mobile its title and description appear together in phase three, the feature row stays 24–27px below the card rather than pinned to the viewport bottom, and short-height overrides prevent overlap. The hero gradient must scale symmetrically from the viewport center at >=1600px so no hard edge appears on Ultrawide screens.

- From "Why ViaBook?" through the footer, viewport reveals are per element rather than section-wide: every heading, card, map/copy group, FAQ row, CTA layer, and footer group stays hidden until that specific element intersects the reveal zone, then reveals once with the established 15%-slower motion. Do not restore section-level `is-visible` observers that reveal later rows early.
- The FAQ opens one item at a time, but may also be fully collapsed. Clicking the currently open question closes it; selecting another question closes the previous item and opens the selected one.
- On phone layouts, the hero product preview remains in normal document flow but is anchored to the bottom of the hero with no floating blank space beneath it; preserve the small horizontal padding and intrinsic preview aspect ratio.
